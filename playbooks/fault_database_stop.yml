- name: fault_database_stop
  hosts: caixa_demo
  become: true
  gather_facts: false
  pre_tasks:
  - ansible.builtin.import_tasks: tasks/guard.yml
  tasks:
  - name: Stop only demo database cluster
    ansible.builtin.systemd_service:
      name: postgresql@16-main
      state: stopped
